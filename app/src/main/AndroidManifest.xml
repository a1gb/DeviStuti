<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guru.devistuti">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".kali" />
        <activity android:name=".durga" />
        <activity android:name=".laxmi" />
        <activity android:name=".saraswati" />
        <activity android:name=".kaliList" />
        <activity android:name=".durgaList" />
        <activity android:name=".laxmiList" />
        <activity android:name=".saraswatiList"></activity>
    </application>

</manifest>